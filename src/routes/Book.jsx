/**
 * Book component that displays a single book's details.
 * @returns {JSX.Element} - The rendered component.
 */
function Book() {
    return ( <div>Book will be here</div> );
}

export default Book;